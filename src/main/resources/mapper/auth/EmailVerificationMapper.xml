<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.cavia.trader.module.auth.repository.EmailVerificationMapper">

    <insert id="saveOrUpdate" parameterType="io.cavia.trader.module.auth.entity.EmailVerification">
        INSERT INTO email_verifications (
        email,
        verification_key,
        expires_at,
        created_at
        )
        VALUES (
        #{email},
        #{verificationKey},
        #{expiresAt},
        #{createdAt}
        )
        ON DUPLICATE KEY UPDATE
        verification_key = VALUES(verification_key),
        expires_at = VALUES(expires_at),
        created_at = VALUES(created_at)
    </insert>

    <select id="findByEmail" parameterType="string" resultType="io.cavia.trader.module.auth.entity.EmailVerification">
        SELECT
        email,
        verification_key,
        expires_at,
        created_at
        FROM
        email_verifications
        WHERE
        email = #{email}
    </select>

</mapper>